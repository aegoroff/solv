post-release-commit-message = "release {{next_version}}"
no-dev-version = false
consolidate-commits = true

post-release-replacements = [
    { file = "../solv/Cargo.toml", min = 0, search = "path = \"\\.\\./solp/\", version = \"[a-z0-9\\.-]+\"", replace = "path = \"../solp/\", version = \"{{next_version}}\"" },
]